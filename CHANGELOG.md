# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.2] - 2026-08-24

### 修复
- **温度条重复叠加与偶发消失**：列表页温度条在同一行被多次插入、或在翻页/路由切换时偶发消失
  - 根因：渲染前未清理既有温度条节点，SPA 路由切换时既有钩子重入导致重复渲染；某些场景下 `MutationObserver` 回调早于数据就绪，温度条被插入后又被清空
  - 修复：渲染前先移除当前行已有的温度条容器，统一渲染入口；观测器回调增加就绪检查，避免空数据覆盖

## [0.2.1] - 2026-08-23

### 修复
- **暗黑模式浅色块 bug**：行内代码（反引号包裹）、代码块、"已解决"等徽章背景在暗黑下呈白色块、文字不可见
  - 根因：暗黑覆盖块漏掉了 `--inline-code-bg`、`--hljs-bg`、`--success-low/-medium/-hover`、`--highlight-bg`、`--danger-low`、`--love-low` 及一批 `-or-` 合成变量，导致回落到亮色主题的浅色值
  - 修复：补齐全部背景类衍生变量，并把亮色下解析为浅色的 `-or-` 合成变量改指向对应深色变量
  - 行内代码对比度由 1.08 提升至 11.20

## [0.2.0] - 2026-08-23

### 新增
- **帖子温度计**：列表页每行显示 0-100 热度条，颜色由绿（冷）到红（热）渐变；热度 = 点赞(40%) + 回复(35%) + 浏览(25%)，按 7 天半衰期做时间衰减
- **随机漫游**：顶栏新增骰子按钮，基于官方 `/sitemap.xml`（约 9 万个话题）随机跳转一个帖子，失败时自动降级到 `/latest.json`
- 点赞徽章的 🔥 火焰标记改为由温度 ≥ 70 触发（原先只看赞数）

## [0.1.0] - 2026-08-23

### 新增
- **暗黑模式一键切换**：顶栏按钮切换，覆盖 60+ 个 Discourse CSS 变量，偏好持久化到 `localStorage`
- **列表页数据增强**：
  - 每行显示点赞数 `❤ N`
  - 已采纳答案的帖子显示 `✓ 已解决` 徽章
- 三路数据源兜底：拦截站内 `fetch` → 解析首屏 `data-preloaded` → 自行请求当前列表页 `.json`
